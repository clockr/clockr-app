const config = {
    API_URL: window.__RUNTIME_CONFIG__.REACT_APP_API_URL,
    APP_TITLE: window.__RUNTIME_CONFIG__.REACT_APP_TITLE,
    APP_NAME: window.__RUNTIME_CONFIG__.REACT_APP_NAME
};

export default config;
